Speaker (Manager): So you will need to do some R&D, right, to figure this out. So if you go the client credential route, wherein you have a user ID, a service account and a password created on the SUR app, which will then give you the token, we’ll have to go that way. We also have to implement a secure access methodology protocol, wherein the connected key, the consumer ID and the password will then need to sit in a vault somewhere.
What I’m proposing is we go with the managed identity route, which Azure supports, wherein there is no token exchange. I do not know how exactly that thing works. That is where you both come in. Please research on how managed identity works and how we can leverage that to get our processes working.
From what I’ve read through the weekend, it seems that managed identity essentially works like a corporate handshake. No keys are exchanged. No tokens are exchanged. It is just that Azure recognizes the application that is built on top of it, and then gives you access to mailboxes. Of course, we’ll have to restrict it using application access policies, etc., to ensure that we are only reading specific group mailboxes and only read items, no write permissions.
But we have to essentially move away from that client credential method. Okay, make it. Yeah, so managed identity is the protocol. Write it down, do your research, make code changes as required.